# Frontend (Vite)
# Base URL for the backend API used in Live mode.
VITE_API_BASE=http://localhost:4000

# Internal demo gate (optional, off by default)
# - Set VITE_ENABLE_DEMO=1 only for internal/demo builds.
# - Demo still requires ?demo=1 in the URL.
# - On non-local hosts, demo only activates when the current hostname is in
#   VITE_DEMO_ALLOWED_HOSTS (comma-separated).
# VITE_ENABLE_DEMO=0
# VITE_DEMO_ALLOWED_HOSTS=demo.shiftway.app
# VITE_SHOW_BACKEND_SETTINGS=0
# VITE_SHOW_DEMO_CONTROLS=0

